---
import "../../styles/global.css";
import { Image } from "astro:assets";
import bitcoinCoreLogo from "../../assets/homepage/bitcoin-core.png";
import btcRpcExplorerLogo from "../../assets/homepage/btc-rpc-explorer.svg";
import electrumLogo from "../../assets/homepage/electrum.svg";
import torLogo from "../../assets/homepage/tor.png";
import Button from "./Button.astro";
import Card from "./Card.astro";
---
<!-- Value Proposition -->
<section class="border-t border-white/5 bg-[#0e0e10]">
  <div class="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8">
    <div class="mx-auto max-w-3xl text-center">
      <h2 class="text-2xl font-bold text-white sm:text-3xl">Run Dojo. Keep your financial metadata off the surveillance web.</h2>
      <p class="mt-4 text-zinc-300">
        Designed for Samourai Wallet, Dojo is the open‑source, self‑hosted backend that puts privacy and sovereignty first—bundling Bitcoin Core, Electrum Server, BTC‑RPC Explorer, and Tor.
      </p>
    </div>
    <dl class="mt-10 grid grid-cols-1 gap-8 sm:grid-cols-2 lg:grid-cols-4">
      <Card>
        <div class="h-28 flex items-center justify-center">
          <Image src={bitcoinCoreLogo} alt="Bitcoin Core" height={80} />
        </div>
        <h3 class="mt-4 text-base font-semibold text-white">Bitcoin Core</h3>
        <p class="mt-2 text-sm sm:text-[15px] text-zinc-400">
          Run your own full node and verify every block and transaction yourself—no third‑party trust.
          Dojo ships with a tuned Core configuration for reliable, long‑running operation.
        </p>
      </Card>
      <Card>
        <div class="h-28 flex items-center justify-center">
          <Image src={electrumLogo} alt="Electrum Server" height={80} />
        </div>
        <h3 class="mt-4 text-base font-semibold text-white">Electrum Server</h3>
        <p class="mt-2 text-sm sm:text-[15px] text-zinc-400">
          Local address indexing for fast wallet sync and history.
          All wallet lookups hit your server only—keeping XPUBs and addresses off remote services.
        </p>
      </Card>
      <Card>
        <div class="h-28 flex items-center justify-center">
          <Image src={btcRpcExplorerLogo} alt="BTC‑RPC Explorer" height={80} />
        </div>
        <h3 class="mt-4 text-base font-semibold text-white">BTC‑RPC Explorer</h3>
        <p class="mt-2 text-sm sm:text-[15px] text-zinc-400">
          A lightweight block explorer served from your node.
          Browse blocks, transactions, and the mempool without leaking metadata to public sites.
        </p>
      </Card>
      <Card>
        <div class="h-28 flex items-center justify-center">
          <Image src={torLogo} alt="Tor" height={80} />
        </div>
        <h3 class="mt-4 text-base font-semibold text-white">Tor Networking</h3>
        <p class="mt-2 text-sm sm:text-[15px] text-zinc-400">
          All services are reachable as Tor onion services by default.
          Keep your IP hidden and access Dojo privately from anywhere.
        </p>
      </Card>
    </dl>
    <div class="mt-10 flex justify-center">
      <Button as="a" href="/install/requirements" type="primary">
        Start the install
      </Button>
    </div>
  </div>
</section>
